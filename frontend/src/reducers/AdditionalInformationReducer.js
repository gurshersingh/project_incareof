const INITIAL_STATE = {
    city:'',
    services:[]
}
const additionalInformation = (state=INITIAL_STATE, action)  =>{
    switch(action.type){

        default:
            return state
    }
}
export default employerRegistrationReducer
